{
  "name": "bolt.chef",
  "version": "1.0.0",
  "description": "Chef + Bolt.diy merged application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p build/client && echo '<!DOCTYPE html><html><head><title>Chef + Bolt.diy</title></head><body><h1>Chef + Bolt.diy API Gateway</h1><p>API endpoints:</p><ul><li>GET /health</li><li>POST /api/unified/applications</li></ul><p>Cloudflare Pages Functions active.</p></body></html>' > build/client/index.html && echo 'Build complete - static output in build/client'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^6.1.5",
    "pg": "^8.11.0",
    "jsonwebtoken": "^9.0.0"
  }
}
